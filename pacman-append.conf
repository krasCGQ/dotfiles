# ffmpeg-git depends on matching libx26{4,5}.so on Extra
# Ignore archlinuxcn package upstream for local rebuild
IgnorePkg = ffmpeg-git

# Xyne's projects
[xyne-x86_64]
Server = https://xyne.archlinux.ca/repos/xyne

# Arch Linux Chinese Community (CDN)
[archlinuxcn]
Server = https://cdn.repo.archlinuxcn.org/$arch
