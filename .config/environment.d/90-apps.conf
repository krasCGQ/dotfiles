# PyTorch: may help with VRAM usage
PYTORCH_HIP_ALLOC_CONF=max_split_size_mb:2048

# Wine: disable debug output by default
WINEDEBUG=-all

# Zstandard: default compression level override
ZSTD_CLEVEL=20
